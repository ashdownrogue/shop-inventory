{
  "name": "shop-inventory",
  "version": "1.0.0",
  "private": true,
  "description": "Local-first garage shop inventory audit. Static, offline-capable, no build step.",
  "scripts": {
    "start": "python3 -m http.server 8899",
    "test": "node tests/smoke.js"
  },
  "devDependencies": {
    "jsdom": "^26.0.0"
  }
}
